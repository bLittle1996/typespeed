/* A function that does precisely no operations (noop) */
export const noop = (): void => {
  /* No operation needs to be done :) */
};

export const differenceBetween = (
  word: string,
  anotherWord: string
): string[] => {
  return [];
};
